!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Rashid 41</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Share+Tech+Mono&display=swap');

    body {
      margin: 0;
      font-family: 'Share Tech Mono', monospace;
      overflow: hidden;
      background: #000;
      color: #FF0000;
    }

    /* MATRIX RAIN CANVAS */
    #matrixCanvas {
      position: fixed;
      top: 0;
      left: 0;
      z-index: 0;
    }

    /* CONTAINER */
    .container {
      position: relative;
      z-index: 2;
      background: rgba(0,0,0,0.9);
      border: 2px solid #FF0000;
      border-radius: 15px;
      box-shadow: 0 0 30px #FF0000;
      padding: 40px;
      max-width: 600px;
      margin: 50px auto;
      text-align: center;
    }

    /* GLITCH HEADING */
    h2 {
      font-size: 2rem;
      color: #FF0000;
      text-shadow: 0 0 5px #FF0000, 0 0 10px #FF5555, 0 0 20px #FFAAAA;
      animation: glitch 1s infinite;
      margin-bottom: 25px;
    }
    @keyframes glitch {
      0% { text-shadow: 2px 0 #FF0000, -2px 2px #FF5555; }
      25% { text-shadow: -2px -2px #FFAAAA, 2px 2px #FF0000; }
      50% { text-shadow: 2px -2px #FF5555, -2px 2px #FFAAAA; }
      75% { text-shadow: -2px 2px #FF0000, 2px -2px #FF5555; }
      100% { text-shadow: 2px 0 #FFAAAA, -2px 2px #FF0000; }
    }

    /* INPUTS & BUTTONS */
    input, .btn {
      width: 90%;
      padding: 12px;
      margin: 10px 0;
      border-radius: 5px;
      border: 1px solid #FF0000;
      background: #000;
      color: #FF0000;
      font-size: 1rem;
      outline: none;
      text-align: center;
      box-shadow: 0 0 10px #FF0000;
      transition: 0.3s;
    }
    input:focus, .btn:hover {
      box-shadow: 0 0 20px #FF5555, 0 0 40px #FFAAAA inset;
    }
    .btn {
      cursor: pointer;
      font-weight: bold;
      text-transform: uppercase;
    }

    /* RESULT CARDS */
    .result {
      margin-top: 20px;
      color: #FF4444;
      border: 1px solid #FF0000;
      padding: 15px;
      border-radius: 5px;
      max-height: 300px;
      overflow-y: auto;
      box-shadow: inset 0 0 20px #FF0000;
    }
    .card {
      background: rgba(0,0,0,0.85);
      border-radius: 10px;
      padding: 15px;
      margin: 10px 0;
      box-shadow: 0 0 20px #FF0000;
      animation: glow 2s infinite alternate;
    }
    @keyframes glow {
      from { box-shadow: 0 0 5px #FF0000; }
      to { box-shadow: 0 0 25px #FF5555; }
    }
    .head { font-weight: bold; color: #FF7777; }
    .loading { font-style: italic; animation: blink 1s infinite; }
    @keyframes blink { 50% { opacity: 0.5; } }

    /* WHATSAPP BUTTON */
    .whatsapp-link {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      margin-top: 15px;
      text-decoration: none;
      color: #FF0000;
      border: 1px solid #FF0000;
      padding: 10px 20px;
      border-radius: 8px;
      font-weight: bold;
      box-shadow: 0 0 15px #FF0000;
    }
    .whatsapp-link:hover {
      background: #FF0000;
      color: #000;
      box-shadow: 0 0 30px #FF5555, 0 0 60px #FFAAAA inset;
    }

    /* OVERLAY */
    .overlay {
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background: rgba(0,0,0,0.95);
      display: none;
      justify-content: center;
      align-items: center;
      z-index: 10;
    }
    .overlay-content {
      background: #000;
      padding: 30px 40px;
      border: 2px solid #FF0000;
      border-radius: 15px;
      text-align: center;
      box-shadow: 0 0 40px #FF0000;
    }
    .overlay-content a {
      display: inline-block;
      margin: 10px 0;
      text-decoration: none;
      color: #FF0000;
      border: 1px solid #FF0000;
      padding: 12px 20px;
      border-radius: 8px;
      font-weight: bold;
      transition: 0.3s;
    }
    .overlay-content a:hover {
      background: #FF0000; color: #000;
    }
    .close-overlay {
      margin-top: 15px;
      display: inline-block;
      cursor: pointer;
      color: #FF0000;
      border: 1px solid #FF0000;
      padding: 5px 12px;
      border-radius: 5px;
    }
    .close-overlay:hover {
      background: #FF0000; color: #000;
    }
  </style>
</head>
<body>
  <canvas id="matrixCanvas"></canvas>

  <!-- Overlay Popup -->
  <div class="overlay" id="whatsappOverlay">
    <div class="overlay-content">
      <h3>Join Our Group</h3>
      <a href="https://chat.whatsapp.com/LMTDgPd91t2GDTGvkilfdV?mode=ems_copy_c" target="_blank">
        <i class="fab fa-whatsapp"></i> Open WhatsApp 
      </a>
      <div class="close-overlay" onclick="closeOverlay()">Close</div>
    </div>
  </div>

  <div class="container">
    <h2>DARK WEB RAIDERS DATA BASE</h2>
    <input type="text" id="num" placeholder="Enter phone number eg.03">
    <button class="btn" onclick="searchNumber()">
      <i class="fas fa-search"></i> Search
    </button>
    <div id="results" class="result"></div>

    <a href="https://chat.whatsapp.com/LMTDgPd91t2GDTGvkilfdV?mode=ems_copy_c" target="_blank" class="whatsapp-link">
      <i class="fab fa-whatsapp"></i> JOIN OUR Group
    </a>
  </div>

  <script>
    /* MATRIX RAIN EFFECT */
    const canvas = document.getElementById("matrixCanvas");
    const ctx = canvas.getContext("2d");
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
    const letters = "0101010010110101010101010101010101010101";
    const fontSize = 20;
    const columns = canvas.width / fontSize;
    const drops = Array(Math.floor(columns)).fill(0);

    function drawMatrix() {
      ctx.fillStyle = "rgba(0,0,0,0.05)";
      ctx.fillRect(0, 0, canvas.width, canvas.height);
      ctx.fillStyle = "#FF0000";
      ctx.font = fontSize + "px monospace";

      for (let i = 0; i < drops.length; i++) {
        const text = letters[Math.floor(Math.random() * letters.length)];
        ctx.fillText(text, i * fontSize, drops[i] * fontSize);

        if (drops[i] * fontSize > canvas.height && Math.random() > 0.975) {
          drops[i] = 0;
        }
        drops[i]++;
      }
    }
    setInterval(drawMatrix, 50);

    /* OVERLAY & API */
    function closeOverlay() {
      document.getElementById('whatsappOverlay').style.display = 'none';
    }

    async function searchNumber() {
      const num = document.getElementById("num").value.trim();
      const resultsDiv = document.getElementById("results");
      resultsDiv.innerHTML = "<p class='loading'>⏳ Searching data...</p>";

      if (!num) {
        alert("Please enter a number!");
        resultsDiv.innerHTML = "";
        return;
      }

      try {
        const response = await fetch(`https://www.3patticenter.com/famofc/api/database.php?num=${encodeURIComponent(num)}`);
        const data = await response.json();

        if (data.status !== "success") {
          resultsDiv.innerHTML = `<p style="color:red;">❌ ${data.message}</p>`;
          return;
        }

        resultsDiv.innerHTML = "";
        data.data.forEach(record => {
          let card = `<div class="card">`;
          for (let key in record) {
            card += `<p><span class="head">${key}:</span> ${record[key]}</p>`;
          }
          card += `</div>`;
          resultsDiv.innerHTML += card;
        });

        // Show overlay after search
        document.getElementById('whatsappOverlay').style.display = 'flex';

      } catch (error) {
        resultsDiv.innerHTML = `<p style="color:red;">⚠️ Error: ${error.message}</p>`;
      }
    }
  </script>
</body>
</html>
